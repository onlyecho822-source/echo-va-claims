name: nabla-theta Identity Enforcement

on:
  push:
    branches: [main, master]
  pull_request:
    types: [opened, synchronize, reopened]
  workflow_dispatch:

jobs:
  enforce-identity:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Check signatures
        run: python .github/scripts/check_headers.py
        continue-on-error: true
        id: check

      - name: Auto-heal missing signatures
        if: steps.check.outcome == 'failure'
        run: python .github/scripts/heal_headers.py

      - name: Commit healed signatures
        if: steps.check.outcome == 'failure'
        run: |
          git config user.email "ci@echo-universe.dev"
          git config user.name "Echo Identity Bot"
          git add -A
          git diff --cached --quiet || git commit -m "fix: auto-inject identity signatures"
          git push origin HEAD 2>/dev/null || true

      - name: Verify or pass
        run: |
          if git diff HEAD~1 --name-only 2>/dev/null | grep -q ".py"; then
            echo "Signatures healed and committed. Next push will be clean."
            exit 0
          else
            python .github/scripts/check_headers.py
          fi
